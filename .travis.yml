language: go
go:
  - 1.11.5
  - 1.12.x
  - master

sudo: required
#script: true
#
#after_success:
#  - bash <(curl -s https://codecov.io/bash)
before_install:
  - go get -t -v ./...

script:
  - go test -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)