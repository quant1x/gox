language: go
go:
  - 1.11.4
  - tip

sudo: required
#script: true
#
#after_success:
#  - bash <(curl -s https://codecov.io/bash)
before_install:
  - go get -t -v ./...

script:
  - go test -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash) -t 81bb74a9-76b5-4cba-b0da-998a252e4cfd